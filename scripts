.env.example
PRIVATE_KEY=your_private_key_here

.gitignore
.env
.secret

scripts/deploy.sh
#!/bin/bash
source .env
forge script script/Deployment.s.sol --rpc-url http://127.0.0.1:8545 --broadcast --private-key $PRIVATE_KEY

test/HelloWorld.t.sol
pragma solidity ^0.8.0;

import "forge-std/Test.sol";

contract HelloWorldTest is Test {
    function testHelloWorld() public {
        assert(true);
    }
}